import { createBdd } from 'playwright-bdd';
// import DashboardPage from '../../pages/Dashboard/DashboardPage';

const { Given, When, Then } = createBdd();

// TODO: Thêm step definitions cho module này
// Tham khảo login.steps.ts để xem pattern chuẩn

// Given('...', async ({ page }) => {
//   await DashboardPage.placeholder(page);
// });

// When('...', async ({ page }) => {
// });

// Then('...', async ({ page }) => {
// });
